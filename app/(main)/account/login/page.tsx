'use client'
import Link from 'next/link'; import { useState } from 'react'; import { useRouter } from 'next/navigation'
export default function AccountLogin() { const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [error,setError]=useState(''); const router=useRouter(); async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok)return setError(d.error);router.push('/account/claim');router.refresh()} const field='w-full rounded-xl border border-gray-200 px-4 py-3'; return <main className="mx-auto max-w-md px-4 py-16"><h1 className="text-3xl font-bold">Institution account</h1><p className="mt-2 text-gray-500">Sign in to claim and manage your school or college profile.</p><form onSubmit={submit} className="mt-8 space-y-4 rounded-2xl border bg-white p-6"><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="Official email" className={field}/><input type="password" required value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" className={field}/>{error&&<p className="text-sm text-red-600">{error}</p>}<button className="w-full rounded-xl bg-primary py-3 font-bold text-white">Sign in</button><p className="text-sm text-gray-500">No account? <Link href="/account/register" className="font-semibold text-primary">Register</Link></p></form></main> }
