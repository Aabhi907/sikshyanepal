'use client'
import Link from 'next/link'; import { useState } from 'react'; import { useRouter } from 'next/navigation'
export default function Register(){const [form,setForm]=useState({full_name:'',email:'',password:''});const [error,setError]=useState('');const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await r.json();if(!r.ok)return setError(d.error);if(d.confirmation_required){setError('Check your email to confirm the account, then sign in.');return}router.push('/account/claim')}const field='w-full rounded-xl border border-gray-200 px-4 py-3';return <main className="mx-auto max-w-md px-4 py-16"><h1 className="text-3xl font-bold">Create institution account</h1><p className="mt-2 text-gray-500">Registration does not grant control of a profile. Every claim is manually verified.</p><form onSubmit={submit} className="mt-8 space-y-4 rounded-2xl border bg-white p-6"><input required value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})} placeholder="Your full name" className={field}/><input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="Official institution email" className={field}/><input type="password" minLength={10} required value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Password (10+ characters)" className={field}/>{error&&<p className="text-sm text-amber-700">{error}</p>}<button className="w-full rounded-xl bg-primary py-3 font-bold text-white">Create account</button><p className="text-sm text-gray-500">Already registered? <Link href="/account/login" className="font-semibold text-primary">Sign in</Link></p></form></main>}
