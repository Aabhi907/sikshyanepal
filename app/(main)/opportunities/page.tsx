import type { Metadata } from 'next'
import { ExternalLink, ShieldCheck, Sparkles } from 'lucide-react'
import { createServerSupabaseClient } from '@/lib/supabase'

export const metadata: Metadata = { title: 'Skills & Opportunities', description: 'Verified internships, fellowships, competitions, courses and student opportunities in Nepal.' }
type Opportunity = { id:string; title:string; opportunity_type:string; organisation:string; location:string|null; eligibility:string|null; summary:string; application_url:string; deadline:string|null; is_verified:boolean; last_verified_at:string|null }
const date=(value:string|null)=>value?new Intl.DateTimeFormat('en-NP',{day:'numeric',month:'short',year:'numeric'}).format(new Date(value)):'Rolling / see source'
export default async function OpportunitiesPage(){const {data}=await createServerSupabaseClient().from('student_opportunities').select('id,title,opportunity_type,organisation,location,eligibility,summary,application_url,deadline,is_verified,last_verified_at').eq('is_published',true).order('deadline',{ascending:true,nullsFirst:false}).limit(60);const items=(data||[]) as Opportunity[];return <main className="min-h-screen bg-[#f8f7f3]"><section className="border-b border-[#e6e4df] bg-[#16233f] text-white"><div className="mx-auto max-w-6xl px-4 py-14 sm:px-6"><p className="text-xs font-bold uppercase tracking-[.14em] text-blue-200">Skills & opportunities</p><h1 className="mt-3 max-w-3xl font-display text-4xl font-bold sm:text-5xl">Practical experience belongs in every student&apos;s plan.</h1><p className="mt-4 max-w-2xl text-lg leading-8 text-blue-100">Verified internships, fellowships, competitions, projects, volunteering and skills routes. Every listing should point to its official source.</p></div></section><div className="mx-auto max-w-6xl px-4 py-9 sm:px-6"><div className="mb-7 flex gap-3 rounded-xl border border-[#e6e4df] bg-white p-4 text-sm leading-6 text-slate-700"><ShieldCheck className="h-5 w-5 shrink-0 text-primary"/><p><strong>Trust rule:</strong> SikshyaNepal does not accept anonymous listings. Check the official application link, eligibility and deadline before sharing personal information or paying any fee.</p></div>{items.length?<div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{items.map(item=><article key={item.id} className="rounded-xl border border-[#e6e4df] bg-white p-5"><p className="text-xs font-bold uppercase tracking-[.12em] text-[#9a302c]">{item.opportunity_type}</p><h2 className="mt-2 font-display text-xl font-bold">{item.title}</h2><p className="mt-1 text-sm font-semibold text-primary">{item.organisation}{item.location?` · ${item.location}`:''}</p><p className="mt-3 text-sm leading-6 text-slate-600">{item.summary}</p>{item.eligibility&&<p className="mt-3 text-xs leading-5 text-slate-500"><strong>Eligibility:</strong> {item.eligibility}</p>}<div className="mt-4 flex items-center justify-between gap-2 border-t border-[#e6e4df] pt-3 text-xs text-slate-500"><span>Deadline: {date(item.deadline)}</span>{item.is_verified&&<span className="font-bold text-emerald-700">Verified</span>}</div><a href={item.application_url} target="_blank" rel="noreferrer" className="mt-4 inline-flex items-center gap-1 text-sm font-bold text-primary hover:underline">Official application <ExternalLink className="h-4 w-4"/></a></article>)}</div>:<section className="rounded-xl border border-dashed border-[#cfcac1] bg-white p-10 text-center"><Sparkles className="mx-auto h-8 w-8 text-primary"/><h2 className="mt-4 font-display text-2xl font-bold">Verified opportunities are being prepared</h2><p className="mx-auto mt-2 max-w-xl text-sm leading-6 text-slate-600">We will publish opportunities only after an editor checks the official source, eligibility and link. Check back soon, or explore Career Explorer while this directory grows.</p></section>}</div></main>}
