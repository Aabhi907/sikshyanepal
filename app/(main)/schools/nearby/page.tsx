'use client'
import { useState } from 'react'
import Link from 'next/link'
import { LocateFixed, MapPin, Navigation, ShieldCheck } from 'lucide-react'
type School = { id: string; name: string; slug: string; district: string; local_level: string | null; verification_status: string; grades_from: number | null; grades_to: number | null; distance_km: number }
export default function NearbySchoolsPage() { const [items, setItems] = useState<School[]>([]); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); function find() { if (!navigator.geolocation) return setError('Location is not supported by this browser.'); setLoading(true); setError(''); navigator.geolocation.getCurrentPosition(async position => { try { const response = await fetch(`/api/schools/nearby?lat=${position.coords.latitude}&lng=${position.coords.longitude}`); const data = await response.json(); if (!response.ok) throw new Error(data.error); setItems(data) } catch (reason) { setError(reason instanceof Error ? reason.message : 'Could not find nearby schools.') } finally { setLoading(false) } }, () => { setLoading(false); setError('Location permission was not granted. You can still browse schools by district.') }, { enableHighAccuracy: false, timeout: 10_000, maximumAge: 300_000 }) } return <main className="min-h-screen bg-[#f0f4ff]"><section className="border-b border-gray-200 bg-white"><div className="mx-auto max-w-5xl px-4 py-12 sm:px-6"><p className="text-xs font-bold uppercase tracking-widest text-primary">School discovery</p><h1 className="mt-3 font-display text-3xl font-extrabold text-ink sm:text-4xl">Schools near you</h1><p className="mt-3 max-w-2xl text-gray-500">Find ECD through Grade 10 schools with mapped coordinates, ordered by straight-line distance. Your location is used for this search only and is not saved.</p><button onClick={find} disabled={loading} className="mt-6 inline-flex items-center gap-2 rounded-xl bg-primary px-5 py-3 text-sm font-bold text-white hover:bg-primary-600 disabled:opacity-50"><LocateFixed className="h-4 w-4" />{loading ? 'Finding schools…' : 'Use my location'}</button></div></section><div className="mx-auto max-w-5xl px-4 py-8 sm:px-6">{error && <div className="rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-950">{error} <Link href="/schools" className="font-bold underline">Browse all schools</Link></div>}{items.length > 0 && <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{items.map(school => <Link key={school.id} href={`/schools/${school.slug}`} className="rounded-2xl border border-gray-200 bg-white p-5 transition hover:-translate-y-1 hover:border-primary hover:shadow-lg"><div className="flex items-start justify-between gap-3"><h2 className="font-display text-lg font-bold text-ink">{school.name}</h2><span className="whitespace-nowrap rounded-full bg-blue-50 px-2 py-1 text-xs font-bold text-primary">{school.distance_km < 1 ? `${Math.round(school.distance_km * 1000)} m` : `${school.distance_km.toFixed(1)} km`}</span></div><p className="mt-3 flex items-center gap-1.5 text-sm text-gray-500"><MapPin className="h-4 w-4" />{school.local_level || school.district}</p><p className="mt-2 text-xs text-gray-500">{school.grades_from != null && school.grades_to != null ? `${school.grades_from === 0 ? 'ECD' : `Grade ${school.grades_from}`}–Grade ${school.grades_to}` : 'Level details pending'}</p><p className="mt-4 flex items-center gap-1.5 text-xs text-gray-400"><ShieldCheck className="h-3.5 w-3.5" />Confirm transport and official details before enrolment.</p></Link>)}</div>}{!items.length && !loading && !error && <div className="rounded-2xl border border-dashed border-gray-300 bg-white p-12 text-center"><Navigation className="mx-auto h-10 w-10 text-gray-300" /><h2 className="mt-4 font-display text-xl font-bold text-ink">Find schools around you</h2><p className="mt-2 text-sm text-gray-500">Allow location access to sort mapped schools by distance.</p></div>}</div></main> }
