'use client'
import { Megaphone } from 'lucide-react'; import { useEffect,useState } from 'react'
type A={id:string;title:string;message:string;image_url:string|null}
const blank={title:'',message:'',image_url:'',link_url:'',link_label:'',placement:'banner',is_active:true}
export default function Page(){const[items,setItems]=useState<A[]>([]);const[form,setForm]=useState(blank);const[msg,setMsg]=useState('');const load=()=>fetch('/api/admin/announcements').then(r=>r.json()).then(d=>Array.isArray(d)&&setItems(d));useEffect(()=>{load()},[]);async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/admin/announcements',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await r.json();setMsg(r.ok?'Announcement created.':d.error||'Could not create announcement.');if(r.ok){setForm(blank);load()}}const f='w-full rounded-lg border border-gray-700 bg-gray-900 px-3 py-2 text-sm';return <div className="p-8 text-gray-100"><h1 className="flex items-center gap-2 text-2xl font-bold"><Megaphone className="h-6 w-6 text-amber-400"/>Site announcements</h1><form onSubmit={submit} className="mt-6 max-w-2xl space-y-3 rounded-xl border border-gray-700 bg-gray-800 p-5"><input required value={form.title} onChange={e=>setForm({...form,title:e.target.value})} placeholder="Title" className={f}/><textarea required value={form.message} onChange={e=>setForm({...form,message:e.target.value})} placeholder="Short message" className={f}/><input value={form.image_url} onChange={e=>setForm({...form,image_url:e.target.value})} placeholder="Optional image URL" className={f}/><input value={form.link_url} onChange={e=>setForm({...form,link_url:e.target.value})} placeholder="Optional link" className={f}/><input value={form.link_label} onChange={e=>setForm({...form,link_label:e.target.value})} placeholder="Optional button label" className={f}/><button className="rounded-lg bg-blue-600 px-4 py-2 text-sm font-bold">Publish announcement</button>{msg&&<p className="text-sm text-cyan-300">{msg}</p>}</form><div className="mt-6 space-y-2">{items.map(a=><div key={a.id} className="rounded-lg border border-gray-700 bg-gray-800 p-4">{a.image_url&&<img src={a.image_url} alt="" className="mb-3 h-24 w-full rounded object-cover"/>}<strong>{a.title}</strong><p className="mt-1 text-sm text-gray-400">{a.message}</p></div>)}</div></div>}
