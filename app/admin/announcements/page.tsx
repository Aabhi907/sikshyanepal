'use client'
import { useState } from 'react'
export default function Page(){const[f,setF]=useState({title:'',message:'',image_url:'',link_url:'',link_label:'',placement:'banner',is_active:true});const[m,setM]=useState('');async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/admin/announcements',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});setM(r.ok?'Published.':'Could not publish.')}const c='w-full rounded-lg border border-gray-700 bg-gray-900 p-3 text-sm';return <div className="p-8 text-white"><h1 className="text-2xl font-bold">Site announcements</h1><form onSubmit={submit} className="mt-6 max-w-xl space-y-3 rounded-xl bg-gray-800 p-5"><input required className={c} placeholder="Title" onChange={e=>setF({...f,title:e.target.value})}/><textarea required className={c} placeholder="Message" onChange={e=>setF({...f,message:e.target.value})}/><input className={c} placeholder="Optional image URL" onChange={e=>setF({...f,image_url:e.target.value})}/><input className={c} placeholder="Optional link" onChange={e=>setF({...f,link_url:e.target.value})}/><select className={c} value={f.placement} onChange={e=>setF({...f,placement:e.target.value})}><option value="banner">Top banner</option><option value="popup">Small popup</option></select><button className="rounded-lg bg-blue-600 px-4 py-2 font-bold">Publish</button>{m&&<p>{m}</p>}</form></div>}
