import{notFound}from'next/navigation';import ProgramForm from'@/components/admin/ProgramForm';import{createAdminSupabaseClient}from'@/lib/supabase';import type{Program}from'@/types';export default async function Page({params}:{params:{id:string}}){const{data}=await createAdminSupabaseClient().from('programs').select('*').eq('id',params.id).single();if(!data)notFound();return <ProgramForm initialData={data as Program} id={params.id}/>}
