'use client'
import Link from'next/link';import{useEffect,useState}from'react';import type{Program}from'@/types'
export default function ProgramsAdmin(){const[items,setItems]=useState<Program[]>([]);useEffect(()=>{fetch('/api/admin/programs').then(r=>r.json()).then(d=>Array.isArray(d)&&setItems(d))},[]);return <div className="p-8 text-white"><div className="flex justify-between"><div><h1 className="text-2xl font-bold">Programs</h1><p className="text-gray-400">Maintain source-backed program discovery content.</p></div><Link href="/admin/programs/new" className="rounded bg-blue-600 px-4 py-2">Add program</Link></div><div className="mt-6 divide-y divide-gray-700 rounded-xl bg-gray-800">{items.map(x=><Link key={x.id} href={`/admin/programs/${x.id}/edit`} className="flex justify-between p-4"><span>{x.name}</span><span className="capitalize text-gray-400">{x.degree_level}</span></Link>)}</div></div>}
