import { NextResponse } from 'next/server'; import { getAuthContext } from '@/lib/auth'; import { createAdminSupabaseClient } from '@/lib/supabase'
export async function GET(_:Request,{params}:{params:{id:string}}){const auth=await getAuthContext();if(!auth)return NextResponse.json({saved:false},{status:401});const{data,error}=await createAdminSupabaseClient().from('saved_colleges').select('id').eq('user_id',auth.user.id).eq('college_id',params.id).maybeSingle();if(error){console.error('[saved-colleges:check]',error);return NextResponse.json({error:'Your shortlist could not be checked.'},{status:500})}return NextResponse.json({saved:Boolean(data)},{headers:{'Cache-Control':'private, no-store'}})}
export async function POST(_:Request,{params}:{params:{id:string}}){const auth=await getAuthContext();if(!auth)return NextResponse.json({error:'Sign in required'},{status:401});const{error}=await createAdminSupabaseClient().from('saved_colleges').upsert({user_id:auth.user.id,college_id:params.id},{onConflict:'user_id,college_id'});if(error){console.error('[saved-colleges:add]',error);return NextResponse.json({error:'This college could not be saved.'},{status:500})}return NextResponse.json({saved:true})}
export async function DELETE(_:Request,{params}:{params:{id:string}}){const auth=await getAuthContext();if(!auth)return NextResponse.json({error:'Sign in required'},{status:401});const{error}=await createAdminSupabaseClient().from('saved_colleges').delete().eq('user_id',auth.user.id).eq('college_id',params.id);if(error){console.error('[saved-colleges:remove]',error);return NextResponse.json({error:'This college could not be removed.'},{status:500})}return NextResponse.json({saved:false})}
