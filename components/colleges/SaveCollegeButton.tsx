'use client'
import { Bookmark } from 'lucide-react'; import { useEffect,useState } from 'react'; import { usePathname, useRouter } from 'next/navigation'
export default function SaveCollegeButton({collegeId}:{collegeId:string}){const[saved,setSaved]=useState(false);const[loading,setLoading]=useState(false);const[error,setError]=useState('');const router=useRouter();const pathname=usePathname();useEffect(()=>{fetch(`/api/saved-colleges/${collegeId}`,{cache:'no-store'}).then(r=>r.ok?r.json():null).then(d=>d&&setSaved(d.saved)).catch(()=>{})},[collegeId]);async function toggle(){setLoading(true);setError('');try{const r=await fetch(`/api/saved-colleges/${collegeId}`,{method:saved?'DELETE':'POST'});if(r.status===401){router.push(`/account/login?next=${encodeURIComponent(pathname)}`);return}if(!r.ok)throw new Error();setSaved(!saved)}catch{setError('Could not update shortlist.')}finally{setLoading(false)}}return <div><button type="button" onClick={toggle} disabled={loading} aria-pressed={saved} className={`inline-flex items-center gap-2 rounded-xl border px-4 py-2.5 text-sm font-semibold disabled:opacity-60 ${saved?'border-blue-600 bg-blue-50 text-blue-700':'border-gray-200 bg-white text-gray-700'}`}><Bookmark className={`h-4 w-4 ${saved?'fill-current':''}`}/>{loading?'Updating…':saved?'Saved':'Save college'}</button>{error&&<p role="alert" className="mt-1 text-xs text-red-600">{error}</p>}</div>}
