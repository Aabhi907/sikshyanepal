'use client'
import Script from 'next/script'
import { useEffect,useState } from 'react'
import { CONSENT_KEY,ConsentChoice } from './CookieConsent'
export default function ConsentScripts(){const[choice,setChoice]=useState({analytics:false,marketing:false});useEffect(()=>{const read=(next?:ConsentChoice)=>{try{const saved=next||JSON.parse(localStorage.getItem(CONSENT_KEY)||'{}');setChoice({analytics:Boolean(saved.analytics),marketing:Boolean(saved.marketing)})}catch{setChoice({analytics:false,marketing:false})}};read();const changed=(event:Event)=>read((event as CustomEvent<ConsentChoice>).detail);window.addEventListener('sn-consent-changed',changed);return()=>window.removeEventListener('sn-consent-changed',changed)},[]);const client=process.env.NEXT_PUBLIC_ADSENSE_CLIENT_ID;const measurementId=process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID;return <>{choice.analytics&&measurementId&&<><Script src={`https://www.googletagmanager.com/gtag/js?id=${measurementId}`} strategy="afterInteractive"/><Script id="sn-google-analytics" strategy="afterInteractive">{`window.dataLayer=window.dataLayer||[];function gtag(){dataLayer.push(arguments)}gtag('js',new Date());gtag('config','${measurementId}',{anonymize_ip:true});`}</Script></>}{choice.marketing&&client&&<Script async src={`https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=${client}`} crossOrigin="anonymous" strategy="afterInteractive"/>}</>}
