'use client'

import { CheckCircle2, Loader2 } from 'lucide-react'
import { FormEvent, useState } from 'react'

const provinces = ['Koshi', 'Madhesh', 'Bagmati', 'Gandaki', 'Lumbini', 'Karnali', 'Sudurpashchim']
const levels = [['pre_primary', 'ECD / Pre-primary'], ['basic', 'Basic (Grades 1–8)'], ['secondary', 'Secondary (Grades 9–10)'], ['multiple', 'Multiple levels (up to Grade 10)']]
const ownership = [['community', 'Community'], ['institutional', 'Private / Institutional'], ['public', 'Public'], ['religious', 'Religious'], ['other', 'Other']]
const mediums = ['English', 'Nepali', 'Both']
const initial = { name: '', province: '', district: '', local_level: '', address: '', ownership_type: '', school_level: '', grades_from: '0', grades_to: '10', phone: '', email: '', official_website: '', description: '', submitter_name: '', submitter_role: '', submitter_contact: '', mediums: [] as string[] }

export default function SubmitSchoolForm() {
  const [form, setForm] = useState(initial); const [message, setMessage] = useState(''); const [loading, setLoading] = useState(false); const [success, setSuccess] = useState(false)
  const set = (key: keyof typeof initial, value: string | string[]) => setForm((current) => ({ ...current, [key]: value }))
  async function submit(event: FormEvent) { event.preventDefault(); setLoading(true); setMessage(''); try { const response = await fetch('/api/submit-school', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(form) }); const data = await response.json(); if (!response.ok) throw new Error(data.error); setSuccess(true); setForm(initial) } catch (error) { setMessage(error instanceof Error ? error.message : 'Could not send your submission.') } finally { setLoading(false) } }
  const field = 'mt-1.5 w-full rounded-xl border border-gray-200 px-3.5 py-2.5 text-sm outline-none focus:border-primary focus:ring-2 focus:ring-primary/10'; const label = 'block text-sm font-semibold text-ink'
  if (success) return <div className="rounded-2xl border border-gray-200 bg-white p-12 text-center"><CheckCircle2 className="mx-auto h-12 w-12 text-emerald-600" /><h2 className="mt-4 font-display text-xl font-bold text-ink">School submitted for review</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-gray-500">Thank you. We will verify the information and publish it only after review.</p><button onClick={() => setSuccess(false)} className="mt-6 font-bold text-primary hover:underline">Submit another school</button></div>
  return <form onSubmit={submit} className="space-y-6"><section className="rounded-2xl border border-gray-200 bg-white p-6 sm:p-8"><h2 className="font-display text-lg font-bold text-ink">School information</h2><div className="mt-6 grid gap-5 sm:grid-cols-2"><label className={`${label} sm:col-span-2`}>School name *<input required value={form.name} onChange={(event) => set('name', event.target.value)} className={field} /></label><label className={label}>Province *<select required value={form.province} onChange={(event) => set('province', event.target.value)} className={field}><option value="">Choose province</option>{provinces.map((item) => <option key={item}>{item}</option>)}</select></label><label className={label}>District *<input required value={form.district} onChange={(event) => set('district', event.target.value)} className={field} /></label><label className={label}>Municipality / rural municipality<input value={form.local_level} onChange={(event) => set('local_level', event.target.value)} className={field} /></label><label className={label}>School type<select value={form.ownership_type} onChange={(event) => set('ownership_type', event.target.value)} className={field}><option value="">Choose type</option>{ownership.map(([value, text]) => <option key={value} value={value}>{text}</option>)}</select></label><label className={label}>School level *<select required value={form.school_level} onChange={(event) => set('school_level', event.target.value)} className={field}><option value="">Choose level</option>{levels.map(([value, text]) => <option key={value} value={value}>{text}</option>)}</select></label><div className="grid grid-cols-2 gap-3"><label className={label}>From *<select value={form.grades_from} onChange={(event) => set('grades_from', event.target.value)} className={field}><option value="0">ECD</option>{Array.from({ length: 10 }, (_, index) => index + 1).map((item) => <option key={item} value={item}>Grade {item}</option>)}</select></label><label className={label}>To *<select value={form.grades_to} onChange={(event) => set('grades_to', event.target.value)} className={field}>{Array.from({ length: 11 }, (_, index) => index).map((item) => <option key={item} value={item}>{item === 0 ? 'ECD' : `Grade ${item}`}</option>)}</select></label></div><label className={`${label} sm:col-span-2`}>Address<input value={form.address} onChange={(event) => set('address', event.target.value)} className={field} /></label></div></section><section className="rounded-2xl border border-gray-200 bg-white p-6 sm:p-8"><h2 className="font-display text-lg font-bold text-ink">Contact and learning information</h2><div className="mt-5 grid gap-5 sm:grid-cols-2"><label className={label}>Phone<input value={form.phone} onChange={(event) => set('phone', event.target.value)} className={field} /></label><label className={label}>School email<input type="email" value={form.email} onChange={(event) => set('email', event.target.value)} className={field} /></label><label className={`${label} sm:col-span-2`}>Official website<input type="url" value={form.official_website} onChange={(event) => set('official_website', event.target.value)} placeholder="https://" className={field} /></label><div className="sm:col-span-2"><p className={label}>Medium of instruction</p><div className="mt-2 flex gap-3">{mediums.map((item) => <label key={item} className="flex items-center gap-2 text-sm"><input type="checkbox" checked={form.mediums.includes(item)} onChange={() => set('mediums', form.mediums.includes(item) ? form.mediums.filter((value) => value !== item) : [...form.mediums, item])} />{item}</label>)}</div></div><label className={`${label} sm:col-span-2`}>Short description<textarea value={form.description} onChange={(event) => set('description', event.target.value)} rows={4} className={field} /></label></div></section><section className="rounded-2xl border border-gray-200 bg-white p-6 sm:p-8"><h2 className="font-display text-lg font-bold text-ink">Your verification contact</h2><p className="mt-1 text-sm text-gray-500">This is used only to check the submission; it is not published.</p><div className="mt-5 grid gap-5 sm:grid-cols-2"><label className={label}>Your name *<input required value={form.submitter_name} onChange={(event) => set('submitter_name', event.target.value)} className={field} /></label><label className={label}>Your role *<input required value={form.submitter_role} onChange={(event) => set('submitter_role', event.target.value)} placeholder="e.g. Parent or school staff" className={field} /></label><label className={`${label} sm:col-span-2`}>Email or phone *<input required value={form.submitter_contact} onChange={(event) => set('submitter_contact', event.target.value)} className={field} /></label></div></section>{message && <p className="rounded-xl bg-red-50 p-4 text-sm text-red-700">{message}</p>}<button disabled={loading} className="inline-flex items-center gap-2 rounded-xl bg-primary px-6 py-3 font-bold text-white disabled:opacity-60">{loading && <Loader2 className="h-4 w-4 animate-spin" />}{loading ? 'Submitting…' : 'Submit school for review'}</button></form>
}
